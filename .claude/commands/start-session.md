---
description: Start a coding session — reads harness state and selects the next feature
---

Run `bash init.sh`. Read `claude-progress.txt` and `feature_list.json`.
Run `git log --oneline -20`. Select the next failing feature by priority (compliance before polish).
State the feature id, regulatory tags, and your implementation plan. Do not start a second feature.
