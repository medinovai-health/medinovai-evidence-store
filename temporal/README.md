# Temporal configuration (reference)

- `dynamicconfig.yaml` — sample dynamic config for self-hosted Temporal clusters. The bundled `docker-compose.yml` uses `temporalio/auto-setup` defaults without mounting this file; copy settings into your server or Helm chart as needed.
